run = "uvicorn backend.main:app --host 0.0.0.0 --port 3000"

[nix]
channel = "stable-23.05"

[deployment]
run = ["sh", "-c", "cd test-vite && npm install && npm run build && cd .. && uvicorn backend.main:app --host 0.0.0.0 --port 3000"]

[env]
PYTHONPATH = "/home/runner/my_assistant"
DATABASE_PATH = "/home/runner/my_assistant/data/agent_memory.db" 